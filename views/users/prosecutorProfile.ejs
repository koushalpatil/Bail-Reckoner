<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Prosecutor Profile Dashboard</title>
    <link rel="stylesheet" href="/path/to/your/styles.css">
    <style>
        body {
            font-family: 'Nunito', sans-serif;
            background-color: #f0f4f8;
            margin: 0;
            padding: 0;
            color: #333;
            overflow-x: hidden;
        }

        .container {
            display: flex;
            height: 100vh;
        }

        .sidebar {
            width: 250px;
            background-color: #007bff;
            box-shadow: 2px 0 5px rgba(0, 0, 0, 0.1);
            padding: 20px;
            transition: all 0.3s ease;
        }

        .sidebar h2 {
            font-size: 1.8em;
            color: #fff;
            margin-bottom: 30px;
            text-align: center;
            font-weight: 700;
        }

        .sidebar ul {
            list-style: none;
            padding: 0;
        }

        .sidebar ul li {
            margin-bottom: 15px;
            transition: transform 0.2s;
        }

        .sidebar ul li:hover {
            transform: scale(1.05);
        }

        .sidebar ul li a {
            text-decoration: none;
            font-size: 1.2em;
            color: #fff;
            display: flex;
            align-items: center;
            transition: color 0.3s ease;
        }

        .sidebar ul li a:hover {
            color: #ffd700;
        }

        .dashboard {
            flex-grow: 1;
            padding: 40px;
            background-color: #f0f4f8;
            overflow-y: auto;
        }

        .dashboard-header {
            margin-bottom: 30px;
            animation: fadeIn 1s ease;
        }

        .dashboard-header h1 {
            font-size: 2.5em;
            color: #333;
            font-weight: 700;
            margin: 0;
        }

        .card-container {
            display: grid;
            grid-template-columns: repeat(auto-fill, minmax(300px, 1fr));
            gap: 20px;
        }

        .card {
            background-color: #fff;
            padding: 30px;
            border-radius: 12px;
            box-shadow: 0 8px 16px rgba(0, 0, 0, 0.1);
            text-align: center;
            transition: transform 0.3s, box-shadow 0.3s ease;
        }

        .card:hover {
            transform: translateY(-10px);
            box-shadow: 0 16px 32px rgba(0, 0, 0, 0.2);
        }

        .card h3 {
            font-size: 1.5em;
            margin-bottom: 10px;
            font-weight: 600;
            color: #333;
        }

        .card p {
            font-size: 1.8em;
            color: #007bff;
            font-weight: 700;
        }

        /* Chargesheet Button */
        .create-chargesheet-button {
            background-color: #fff;
            padding: 30px;
            border-radius: 12px;
            box-shadow: 0 8px 16px rgba(0, 0, 0, 0.1);
            text-align: center;
            transition: transform 0.3s, box-shadow 0.3s ease;
            animation: fadeInUp 0.7s ease forwards;
            margin-top: 30px;
        }

        .create-chargesheet-button:hover {
            transform: translateY(-10px);
            box-shadow: 0 16px 32px rgba(0, 0, 0, 0.2);
        }

        .create-chargesheet-button h3 {
            font-size: 2em;
            font-weight: 700;
            margin-bottom: 20px;
            color: #333;
        }

        .create-chargesheet-button a {
            padding: 10px 20px;
            border-radius: 5px;
            background-color: #007bff;
            color: #fff;
            font-size: 1.2em;
            text-decoration: none;
            transition: background-color 0.3s;
        }

        .create-chargesheet-button a:hover {
            background-color: #0056b3;
        }

        /* Animations */
        @keyframes fadeIn {
            from { opacity: 0; }
            to { opacity: 1; }
        }

        @keyframes fadeInUp {
            from {
                opacity: 0;
                transform: translateY(20px);
            }
            to {
                opacity: 1;
                transform: translateY(0);
            }
        }
    </style>
</head>
<body>
    <div class="container">
        <!-- Sidebar -->
        <div class="sidebar">
            <h2>Prosecutor Dashboard</h2>
            <ul>
                <li><a href="#">Dashboard</a></li>
                <li><a href="#">Profile</a></li>
                <li><a href="#">Cases</a></li>
                <li><a href="#">Chargesheet</a></li>
                <li><a href="#">Settings</a></li>
            </ul>
        </div>

        <!-- Main Dashboard -->
        <div class="dashboard">
            <!-- Prosecutor Information -->
            <div class="dashboard-header">
                <h1><%= prosecutor.name %>'s Dashboard</h1>
            </div>
            
            <!-- Cards Display -->
            <div class="card-container">
                <div class="card">
                    <h3>Username</h3>
                    <p><%= prosecutor.username %></p>
                </div>
                <div class="card">
                    <h3>Role</h3>
                    <p><%= prosecutor.role %></p>
                </div>
                <div class="card">
                    <h3>Email</h3>
                    <p><%= prosecutor.contactDetails.email %></p>
                </div>
                <div class="card">
                    <h3>Contact</h3>
                    <p><%= prosecutor.contactDetails.phone %></p>
                </div>
                <div class="card">
                    <h3>Police Station</h3>
                    <p><%= prosecutor.policeStation %></p>
                </div>
                <div class="card">
                    <h3>Police ID</h3>
                    <p><%= prosecutor.policeId %></p>
                </div>
            </div>
            
            <!-- Button for Filing New Chargesheet -->
            <div class="create-chargesheet-button">
                <h3>File a Chargesheet</h3>
                <a href="/chargesheet">File New Chargesheet</a>
            </div>
        </div>
    </div>
</body>
</html>