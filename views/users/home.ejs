<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="/path/to/your/style.css">
    <title>Bail Reckoner</title>
    <style>
        body {
            font-family: "Poppins", sans-serif;
            display: flex;
            justify-content: center;
            align-items: center;
            height: 100vh;
            margin: 0;
            position: relative;
            overflow: hidden;
            background: linear-gradient(to right, #d4e1f1, #a8c0e8);
            animation: backgroundChange 8s ease-in-out infinite;
        }

        @keyframes backgroundChange {
            0% {
                background: linear-gradient(to right, #d4e1f1, #a8c0e8);
            }
            25% {
                background: linear-gradient(to right, #a8c0e8, #d4e1f1);
            }
            50% {
                background: linear-gradient(to right, #d4e1f1, #a8c0e8);
            }
            75% {
                background: linear-gradient(to right, #a8c0e8, #d4e1f1);
            }
            100% {
                background: linear-gradient(to right, #d4e1f1, #a8c0e8);
            }
        }

        .container {
            background: rgba(255, 255, 255, 0.85);
            padding: 2rem;
            border-radius: 12px;
            box-shadow: 0 6px 12px rgba(0, 0, 0, 0.2);
            text-align: center;
            width: 100%;
            max-width: 1000px;
            display: flex;
            flex-direction: row;
            align-items: center;
            justify-content: space-between;
            position: relative;
            z-index: 1;
            overflow: hidden;
            /* Pseudo-element for inner square border */
        }

        .container::before {
            content: "";
            position: absolute;
            top: 10px;
            left: 10px;
            right: 10px;
            bottom: 10px;
            border: 2px solid #333;
            border-radius: 8px;
            pointer-events: none;
            z-index: -1;
        }

        .image-section {
            flex: 1;
            margin-right: 1rem;
        }

        .image-section img {
            width: 100%;
            max-width: 400px;
            border-radius: 8px;
        }

        .text-section {
            flex: 1;
            text-align: center;
        }

        h1 {
            font-size: 3rem; /* Adjusted size for better visibility */
            font-family: "Arial", sans-serif; /* Changed font */
            font-weight: bold; /* Make text bold */
           /* Added border */
            border-radius: 8px;
            padding: 0.5rem 1rem;
            margin-bottom: 1.5rem;
            color: #333;
            display: inline-block; /* Added for border */
            background: rgba(255, 255, 255, 0.7); /* Background color to make text stand out */
        }

        .form-group {
            margin-bottom: 1rem;
        }

        button {
            background: linear-gradient(45deg, #d4e1f1, #a8c0e8);
            border: 2px solid black;
            color: black;
            padding: 0.75rem 1.5rem;
            text-align: center;
            text-decoration: none;
            display: inline-block;
            font-size: 1rem;
            margin: 0.5rem;
            cursor: pointer;
            border-radius: 50px;
            transition: background 0.3s ease-in-out, transform 0.3s ease-in-out, color 0.3s ease-in-out;
        }

        button:hover {
            background: linear-gradient(45deg, #a8c0e8, #d4e1f1);
            color: white;
            transform: scale(1.05);
        }

        a {
            display: inline-block;
            margin-top: 1rem;
            color: #5995fd;
            text-decoration: none;
            font-weight: 600;
            transition: color 0.3s ease-in-out;
        }

        a:hover {
            color: #4d84e2;
        }

        .squares {
            position: absolute;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            z-index: 0;
            overflow: hidden;
        }

        .squares div {
            position: absolute;
            width: 100px;
            height: 100px;
            background: rgba(0, 0, 0, 0.1);
            animation: squareMove 2s infinite;
        }

        .squares div:nth-child(1) {
            top: 10%;
            left: 10%;
            animation-delay: 0s;
        }

        .squares div:nth-child(2) {
            top: 20%;
            left: 20%;
            animation-delay: 0.5s;
        }

        .squares div:nth-child(3) {
            top: 30%;
            left: 30%;
            animation-delay: 1s;
        }

        .squares div:nth-child(4) {
            top: 40%;
            left: 40%;
            animation-delay: 1.5s;
        }

        @keyframes squareMove {
            0% {
                transform: scale(0.5);
                opacity: 0.5;
            }
            50% {
                transform: scale(1);
                opacity: 0.7;
            }
            100% {
                transform: scale(0.5);
                opacity: 0.5;
            }
        }
    </style>
</head>
<body>
    <div class="squares">
        <div></div>
        <div></div>
        <div></div>
        <div></div>
    </div>
    <div class="container">
        <div class="image-section">
            <img src="https://img.freepik.com/free-vector/supreme-court-concept-illustration_114360-13313.jpg?t=st=1725482255~exp=1725485855~hmac=4ba8d911039e464d88bbd29008b875ac042e3c0826ced9c4cfac697ee13fd831&w=740" alt="Court Illustration">
        </div>
        <div class="text-section">
            <h1>Bail Reckoner</h1>
            <% if (!isLogedIn) { %>
                <div class="form-group">
                    <form action="/login">
                        <button>Login</button>
                    </form>
                </div>
                <div class="form-group">
                    <form action="/signup">
                        <button>SignUp</button>
                    </form>
                </div>
            <% } else { %>
                <div class="form-group">
                    <form action="/logout" method="post">
                
                        <button>Logout</button>
                    </form>
                </div>
            <% } %>
            <div class="form-group">
                <form action="/allcases">
                    <button>View All Cases</button>
                </form>
            </div>
        </div>
    </div>
</body>
</html>
